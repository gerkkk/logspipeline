apiVersion: v2
name: argocd
description: Wrapper chart for Argo CD — pins the upstream version and overrides.
type: application
version: 0.1.0
appVersion: "v2.11.0"
dependencies:
  - name: argo-cd
    version: "7.7.0"
    repository: https://argoproj.github.io/argo-helm
